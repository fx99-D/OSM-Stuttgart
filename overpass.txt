Seite 5:
/*
This has been generated by the overpass-turbo wizard.
The original search was:
“amenity=restaurant in Böblingen”
*/
[out:json][timeout:25];
// fetch area “Böblingen” to search in
{{geocodeArea:Böblingen}}->.searchArea;
// gather results
(
  // query part for: “amenity=restaurant”
  node["amenity"="restaurant"](area.searchArea);
  way["amenity"="restaurant"](area.searchArea);
  relation["amenity"="restaurant"](area.searchArea);
);
// print results
out body;
>;
out skel qt;

Seite 8:
[out:json][timeout:25];
// fetch area “Böblingen” to search in
{{geocodeArea:Böblingen}}->.searchArea;
// gather results
(
nwr["amenity"="restaurant"][website](area.searchArea);
);
// print results
out body;
>; out skel qt;

Seite 9:
[out:json][timeout:25];
// fetch area “Böblingen” to search in
{{geocodeArea:Böblingen}}->.searchArea;
// gather results
(
nwr["amenity"="restaurant"][website](area.searchArea);
nwr[tourism=hotel][website](area.searchArea);
);
// print results
out body;
>; out skel qt;

Seite 10:
[out:json][timeout:25];
// gather results
(
  // query part for: “amenity=restaurant”
  node["amenity"="restaurant"]({{bbox}});
  way["amenity"="restaurant"]({{bbox}});
  relation["amenity"="restaurant"]({{bbox}});
);
// print results
out body;
>;
out skel qt;

Seite 11:
[out:json][timeout:25];
// fetch area “Böblingen” to search in
{{geocodeArea:Böblingen}}->.searchArea;
// gather results
(
nwr["amenity"="restaurant"][website](area.searchArea);
nwr[tourism=hotel][website](area.searchArea);
);
// print results
out body;
>; out skel qt;

[out:json][timeout:25];
// fetch area “Böblingen” to search in
{{geocodeArea:Böblingen}}->.searchArea;
// gather results
(
nwr[amenity=restaurant](area.searchArea);
- nwr[amenity=restaurant][website](area.searchArea);
);
// print results
out body;
>; out skel qt;

Seite 13:
[out:csv(::id,::type,name,cuisine,website)] [timeout:25];
// fetch area “Böblingen” to search in
{{geocodeArea:Böblingen}}->.searchArea;
// gather results
(
way["amenity"="restaurant"][website](area.searchArea);
);
// print results
out body;
//>; out skel qt;

Seite 14:
[out:json][timeout:25];
// fetch area “Böblingen” to search in
{{geocodeArea:Böblingen}}->.searchArea;
// gather results
(
way["amenity"="restaurant"][website](area.searchArea);
);
// print results
out body;
//>; out skel qt;

Seite 17:
[timeout:180];
nwr[~"^.*:covid19$"~"."]({{bbox}});
out meta;

Beispiel CSS
// speedlimits visualized in Gärtringen
[out:json][timeout:25];

{{geocodeArea:Gärtringen}}->.searchArea;
way[highway~"(residential|unclassified|living_street|tertiary|secondary|primary)"](area.searchArea);

{{style:
  way[highway=living_street]
{ color:red; width:4; opacity:1; }
  way[maxspeed=30]
{ color:orange; width:4; opacity:1; }
  way[maxspeed=50]
{ color:yellow; width:4; opacity:1; }
  way[maxspeed=70]
{ color:green; width:4; opacity:1; } 
  way[maxspeed=80]
{ color:purple; width:4; opacity:1; } 
}}

out body;
>; out skel qt;
